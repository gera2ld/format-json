export enum ItemTypes {
  KEY = 'KEY',
  VALUE = 'VALUE',
  COMMA = 'COMMA',
  BR = 'BR',
  SPACE = 'SPACE',
  COLON = 'COLON',
  BLOCK = 'BLOCK',
}

export enum GroupTypes {
  MULTILINE = 'MULTILINE',
  SINGLELINE = 'SINGLELINE',
  KEY = 'KEY',
}
